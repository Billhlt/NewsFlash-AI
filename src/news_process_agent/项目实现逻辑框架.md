# 新闻处理代理项目 - 详细实现逻辑框架

## 项目概述
本项目是一个基于Vue 3前端和FastAPI后端的新闻自动分类与整理系统，通过AI对新闻内容进行分类，并按照分类结果将新闻保存到对应的Markdown文件中。

---

## 一、系统架构

### 1.1 技术栈
- **前端**: Vue 3 + TypeScript + Vite
- **后端**: FastAPI (Python)
- **AI服务**: SpringAI (端口8081)
- **数据存储**: 本地Markdown文件

### 1.2 服务端口
- **前端开发服务器**: 默认Vite端口（通常5173）
- **SpringAI服务**: `localhost:8081`
- **FastAPI服务**: `localhost:8083`

---

## 二、详细实现流程

### 阶段一：用户输入与数据预处理

#### 步骤1.1：用户输入新闻内容
**位置**: `src/views/SearchPage.vue`

**实现细节**:
- 用户在搜索框（textarea）中输入待整理的新闻内容
- 支持多行输入，自动调整高度（最大120px）
- 支持快捷键提交：
  - `Shift + Enter`
  - `Ctrl + Enter`
- 点击"发送"按钮提交

**数据处理**:
```javascript
// 文本预处理逻辑
1. 将连续的换行符替换为单个换行符: replace(/\n+/g, '\n')
2. 清除末尾的所有空格和换行符: trimEnd()
3. 通过路由参数传递到处理页面
```

**路由跳转**:
- 跳转到 `/textbox` 页面
- 通过 `query.content` 传递处理后的内容

---

### 阶段二：新闻内容解析与AI分类

#### 步骤2.1：接收并初始化数据
**位置**: `src/views/TextBoxPage.vue` - `onMounted` 钩子

**实现细节**:
- 从路由参数 `route.query.content` 获取新闻内容
- 将内容显示在左侧文本框（`leftContent`）

#### 步骤2.2：开始处理（点击"开始"按钮）
**位置**: `src/views/TextBoxPage.vue` - `handleStart` 函数

**数据初始化**:
```javascript
1. 将 leftContent 按换行符分割: split('\n')
2. 存入 newsList: string[]  // 原始新闻内容列表
3. 初始化 analysisList: string[]  // AI分析结果列表（初始为空字符串数组）
4. 显示状态: "步骤一：存入列表-->完成"
```

**数据结构**:
- `newsList`: 存储原始新闻内容，每个元素为一条新闻
- `analysisList`: 存储AI返回的分类编号，索引与newsList对应

#### 步骤2.3：AI分类处理
**位置**: `src/views/TextBoxPage.vue` - `processNewsWithAI` 函数

**实现细节**:
1. **并发请求AI服务**:
   - 为每条新闻创建独立的Promise任务
   - 使用 `Promise.all()` 并发执行所有AI请求
   - 提高处理效率，避免串行等待

2. **AI请求构造**:
   ```javascript
   // 每条新闻的请求格式
   prompt = CategoryAnalysis提示词 + "\n\n新闻内容：" + 新闻内容
   chatId = "news_analysis_" + 索引
   ```

3. **提示词内容** (`src/utils/prompts.js`):
   - 包含17个分类类目的完整列表
   - 要求AI只返回分类编号（1-17），无其他文字

4. **API调用** (`src/api/api.js`):
   - 调用SpringAI服务: `GET /ai/chat`
   - 参数: `prompt`, `chatId`
   - 超时设置: 100秒

5. **结果处理**:
   - 将AI返回的分类编号存入 `analysisList` 对应索引位置
   - 更新状态: "步骤二：AI分析完成"

**错误处理**:
- 捕获异常并显示错误信息
- 错误信息显示在右侧状态框

#### 步骤2.4：可视化展示
**位置**: `src/views/TextBoxPage.vue` - `transformToNewsBars` 函数

**实现细节**:
- 将新闻列表转换为HTML格式的长条形div
- 每个div包含：
  - 分类编号（蓝色小方块显示）
  - 新闻内容（文本显示）
- 使用 `v-html` 渲染到左侧显示区域

**显示格式**:
```html
<div class="news-bar">
  <span class="news-number">分类编号</span>
  <span class="news-content">新闻内容</span>
</div>
```

---

### 阶段三：数据重排与排序

#### 步骤3.1：生成第一个二维列表（TDList01）
**位置**: `src/views/TextBoxPage.vue` - `sort` 函数

**实现细节**:
```javascript
1. 将 analysisList 转换为数字列表: map(Number)
2. 遍历数字列表，生成二维列表:
   TDList01 = [
     [1, 分类编号1],  // [原始索引, 分类编号]
     [2, 分类编号2],
     ...
   ]
```

**数据结构说明**:
- `TDList01`: `number[][]`
- 每个元素: `[原始索引（从1开始）, 分类编号]`
- 用途: 保留原始索引信息，用于后续重排时找回原始新闻内容

#### 步骤3.2：按分类编号排序（生成TDList02）
**实现细节**:
```javascript
1. 复制 TDList01: slice()
2. 按第二个元素（分类编号）升序排序: sort((x, y) => x[1] - y[1])
3. 结果存入 TDList02
```

**排序规则**:
- 按照分类编号（1-17）从小到大排序
- 相同分类的新闻会聚集在一起

#### 步骤3.3：生成最终列表（finallist）
**实现细节**:
```javascript
1. 遍历排序后的 TDList02
2. 根据原始索引从 newsList 中获取对应新闻内容
3. 生成最终列表:
   finallist = [
     [分类编号1, "- 新闻内容1"],
     [分类编号2, "- 新闻内容2"],
     ...
   ]
4. 为每条新闻内容前添加 "- " 前缀（Markdown列表格式）
```

**数据结构**:
- `finallist`: `[number, string][]`
- 格式: `[分类编号, "- 新闻内容"]`
- 已按分类编号排序，可直接用于保存

#### 步骤3.4：更新可视化显示
**实现细节**:
- 重新生成HTML内容，显示排序后的新闻列表
- 更新状态: "步骤三：重排完成"

---

### 阶段四：数据持久化

#### 步骤4.1：调用保存接口（点击"追加"按钮）
**位置**: `src/views/TextBoxPage.vue` - `append` 函数

**实现细节**:
```javascript
1. 调用 saveNews(finallist.value)
2. 更新状态: "步骤四：已保存到本地文件"
```

#### 步骤4.2：前端API请求
**位置**: `src/api/api.js` - `saveNews` 函数

**实现细节**:
- 使用FastAPI客户端（端口8083）
- 请求方法: `POST /api/save-news`
- 请求体格式:
  ```json
  {
    "news_list": [
      [分类编号, "新闻内容"],
      ...
    ]
  }
  ```
- 超时设置: 100秒

#### 步骤4.3：后端处理与文件保存
**位置**: `backend/main.py` - `save_news` 接口

**实现细节**:

1. **数据验证**:
   - 验证每个数据项是否为列表且长度≥2
   - 验证分类编号是否为1-17的整数
   - 验证新闻内容是否为非空字符串

2. **文件映射**:
   - 根据分类编号从 `NAME_MAP` 获取文件名
   - 17个分类对应17个Markdown文件:
     ```
     1. 语音·音频识别与合成-非商业项目.md
     2. 计算机视觉分析-生成模型不算入其中-非商业.md
     3. 大语言模型，agent智能体-非商业.md
     ...
     17. 其他.md
     ```

3. **文件保存**:
   - 目标目录: `/media/bill/新加卷/.../ai技术的突破进展/`
   - 文件操作: 追加模式（`"a"`）
   - 编码: UTF-8
   - 写入格式: `\n{新闻内容}\n`

4. **错误处理**:
   - 收集所有错误信息
   - 返回保存统计:
     ```json
     {
       "success": true,
       "saved_count": 成功数量,
       "total_count": 总数量,
       "errors": [错误列表],
       "message": "成功保存 X 条新闻"
     }
     ```

---

## 三、数据流转图

```
用户输入
  ↓
SearchPage.vue (文本预处理)
  ↓
路由跳转 (query参数传递)
  ↓
TextBoxPage.vue
  ↓
handleStart() → 分割新闻 → newsList[]
  ↓
processNewsWithAI() → 并发请求AI
  ↓
SpringAI (端口8081) → 返回分类编号
  ↓
analysisList[] (分类编号列表)
  ↓
transformToNewsBars() → 可视化显示
  ↓
sort() → 生成TDList01 → 排序 → TDList02 → 生成finallist
  ↓
append() → saveNews()
  ↓
FastAPI (端口8083) → /api/save-news
  ↓
验证数据 → 文件映射 → 写入Markdown文件
  ↓
返回保存结果
```

---

## 四、关键数据结构

### 4.1 前端数据结构

```typescript
// 原始新闻列表
newsList: string[] = ["新闻1", "新闻2", ...]

// AI分类结果列表（索引与newsList对应）
analysisList: string[] = ["3", "1", "5", ...]

// 第一个二维列表：[原始索引, 分类编号]
TDList01: number[][] = [[1, 3], [2, 1], [3, 5], ...]

// 排序后的二维列表
TDList02: number[][] = [[2, 1], [1, 3], [3, 5], ...]

// 最终列表：[分类编号, "- 新闻内容"]
finallist: [number, string][] = [[1, "- 新闻2"], [3, "- 新闻1"], ...]
```

### 4.2 后端数据结构

```python
# 请求模型
class NewsListRequest:
    news_list: List[List]  # [[分类编号, 新闻内容], ...]

# 响应模型
{
    "success": bool,
    "saved_count": int,
    "total_count": int,
    "errors": List[str] | None,
    "message": str
}
```

---

## 五、分类体系

项目使用17个分类类目：

1. 语音·音频识别与合成-非商业项目
2. 计算机视觉分析-生成模型不算入其中-非商业
3. 大语言模型，agent智能体-非商业
4. 图片生成模型-非商业
5. 音频生成模型-非商业
6. 视频生成模型-非商业
7. 3d人物物品生成模型-非商业
8. 3D环境生成模型-世界模型-非商业
9. 多模态ai模型相关-非商业
10. AI 终端技术或项目
11. 模型记忆相关
12. AI安全与对齐
13. Extended Reality和脑机接口
14. 科技趋势分析以及名人言论
15. 评测基准与比赛
16. AI软件创业项目-商业项目
17. 其他

---

## 六、错误处理机制

### 6.1 前端错误处理
- AI请求失败: 捕获异常，显示错误信息
- 数据验证: 检查undefined值，提示用户

### 6.2 后端错误处理
- 数据格式验证: 检查列表格式、类型、范围
- 文件操作错误: 捕获IO异常，记录错误信息
- 返回详细错误列表，便于排查问题

---

## 七、性能优化点

1. **并发AI请求**: 使用 `Promise.all()` 并发处理所有新闻，而非串行
2. **异步操作**: 排序操作使用 `setTimeout` 包装，避免阻塞UI
3. **请求超时**: 设置100秒超时，避免长时间等待
4. **文件追加模式**: 使用追加模式写入，避免覆盖已有内容

---

## 八、用户交互流程

1. **输入阶段**: 用户在SearchPage输入新闻内容
2. **处理阶段**: 点击"开始"按钮，等待AI分析完成
3. **查看阶段**: 查看分类结果和可视化展示
4. **重排阶段**: 点击"重排"按钮，查看排序后的结果
5. **保存阶段**: 点击"追加"按钮，将结果保存到文件
6. **返回阶段**: 点击"返回"按钮，回到输入页面

---

## 九、文件结构说明

```
项目根目录/
├── backend/
│   ├── main.py              # FastAPI后端服务
│   └── data/                # 数据目录（实际路径在代码中配置）
├── src/
│   ├── views/
│   │   ├── SearchPage.vue   # 输入页面
│   │   └── TextBoxPage.vue  # 处理页面
│   ├── api/
│   │   └── api.js           # API调用封装
│   └── utils/
│       └── prompts.js        # AI提示词配置
└── 项目实现逻辑框架.md       # 本文档
```

---

## 十、总结

本项目实现了一个完整的新闻自动分类与整理流程：

1. **输入**: 用户输入多条新闻内容
2. **分类**: AI自动识别每条新闻的分类（1-17）
3. **排序**: 按分类编号对新闻进行排序
4. **保存**: 将排序后的新闻追加到对应的Markdown文件中

整个流程自动化程度高，支持批量处理，具有良好的错误处理和用户反馈机制。

